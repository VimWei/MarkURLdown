# MarkURLdown 依赖包
# 安装命令：
# pip install -r requirements.txt

# 基础依赖
requests>=2.32.0
markitdown>=0.1.3

# 网页解析依赖
beautifulsoup4>=4.13.0
lxml>=5.3.0

# 爬虫框架依赖
# 现代化HTTP客户端，支持HTTP/2，替代requests
httpx>=0.28.0
# 异步HTTP客户端，用于并发图片下载
aiohttp>=3.12.0

# 浏览器自动化（用于复杂反爬虫场景）
# 安装后需要运行: playwright install
playwright>=1.55.0

# GUI 依赖
PySide6>=6.9.0